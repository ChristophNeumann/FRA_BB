function boolVectFixedVariables = getFixingVectorObjInfluence(y, yCheck, originalModel, activeConstraints, k)
    obj_influence = originalModel.obj(*(y
    [influences,indices] = maxk(abs(originalModel.obj),k);
    n = length(remainder);
    freedom_matrix = B*spdiags(remainder(:),0,n,n) + 1/2*abs(B);
    freedom_measure = vecnorm(freedom_matrix,1,1);
    [~,index] = maxk(freedom_measure,k);
    %[~,index] = maxk(freedom_matrix(end,:),k);
    boolVectFixedVariables = indicesToBooleanVector(index,originalModel);
end